#!groovy
// Run docker build
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        dockerfile {
            label 'Dockerfile'
            dir './docker'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'nano --version'
            }
        }
    }
}