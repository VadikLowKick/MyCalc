#!groovy
// Run docker build
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        dockerfile {
            dir './docker'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'nano --version'
            }
        }
    }
}