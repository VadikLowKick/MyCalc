#!groovy
// Run docker build
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        docker { 
            image "mycalc:${env.BUILD_ID}"
         }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}