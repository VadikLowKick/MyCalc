#!groovy
// Run docker build
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            dir './docker'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'ls -l'
            }
        }
    }
}